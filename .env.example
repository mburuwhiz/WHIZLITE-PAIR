# --- WHIZ LITE WhatsApp Linker Environment Variables ---

# The port for the web server to run on.
# Default is 3000 if not specified.
PORT=3000

# The connection URI for your MongoDB database.
# This is required for the application to store session data.
# Example: MONGO_URI="mongodb://localhost:27017/whizlite_sessions"
MONGO_URI=""
